// arrays

let names = ['luigi','mario','yoshi']

names.push('toad')
// names.push(3)

let mixed = ['ken',3,4,'reza']
mixed.push(5)
mixed.push('test')
mixed[0] = 'ahmad'

console.log(mixed)

// objects
let ninja = {
    name: 'mario',
    belt: 'black',
    age: 20
}

ninja.age = 40

console.log(ninja.age)